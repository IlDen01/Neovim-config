# 🚀 Neovim Cheat Sheet

Cборка основана на **Lazy.nvim** + **LSP** + **Treesitter**.
Основная клавиша-модификатор (Leader): **Пробел** (`<Space>`).

---

## ⌨️ Основные сочетания

### Управление редактором
| Клавиша | Описание |
| :--- | :--- |
| `<Space> + <Space>` | **Убрать подсветку поиска** (после `/search`) |
| `//` (в режиме visual) | **Искать выделенный текст** по файлу |

### Вкладки (Bufferline)
| Клавиша | Описание |
| :--- | :--- |
| `<Tab>` | Перейти на **следующую** вкладку |
| `<Shift> + <Tab>` | Перейти на **предыдущую** вкладку |
| `<Space> + x` | **Закрыть** текущую вкладку (буфер) |

---

## 📁 Файловый менеджер (Neo-tree)
Открыть дерево: `<Space> + e`

**Когда фокус внутри дерева:**
| Клавиша | Описание |
| :--- | :--- |
| `Enter` / `l` | Открыть файл или папку |
| `h` | Закрыть папку / Подняться выше |
| `a` | **Создать** файл или папку (введи имя, для папки добавь `/` в конце) |
| `d` | **Удалить** файл/папку |
| `r` | **Переименовать** |
| `y` | Копировать имя файла |
| `?` | Показать все команды помощи |

---

## 🔍 Поиск (Telescope)
| Клавиша | Описание |
| :--- | :--- |
| `<Space> + f + f` | **Найти файл** (по имени) |
| `<Space> + f + g` | **Найти текст** внутри файлов (Live Grep) |
| `<Space> + f + b` | Найти открытый **буфер** (вкладку) |
| `<Space> + f + h` | Поиск по справке (Help tags) |

---

## 🧠 Работа с кодом (LSP & IDE)
Работает, когда подключен языковой сервер (Python, Go, C++, Java и т.д.).

### Навигация
| Клавиша | Описание |
| :--- | :--- |
| `g + d` | **Go to Definition** (Перейти к определению функции/класса) |
| `g + D` | Перейти к декларации |
| `g + r` | **References** (Показать все места, где это используется) |
| `g + i` | Implementation (Показать реализацию интерфейса) |

### Действия
| Клавиша | Описание |
| :--- | :--- |
| `K` (Shift+k) | **Документация** (Всплывающее описание под курсором) |
| `<Space> + r + n` | **ReName** (Умное переименование переменной везде) |
| `<Space> + c + a` | **Code Action** (Быстрые исправления ошибок/импортов) |

### Автодополнение (в меню)
| Клавиша | Описание |
| :--- | :--- |
| `Ctrl + n` / `j` | Выбрать **следующий** вариант |
| `Ctrl + p` / `k` | Выбрать **предыдущий** вариант |
| `Enter` | **Подтвердить** выбор |
| `Ctrl + Space` | Вызвать меню принудительно |

### Комментарии
| Клавиша | Описание |
| :--- | :--- |
| `g + c + c` | Закомментировать **строку** |
| `g + c` (visual) | Закомментировать **выделенный блок** |

---

## 🌳 Git (Fugitive & Gitsigns)

### Основное окно (`:Git`)
| Клавиша | Описание |
| :--- | :--- |
| `-` (минус) | Добавить файл в индекс (Stage) / Убрать из индекса (Unstage) |
| `=` (равно) | Показать изменения (Diff) |
| `cc` | Сделать **Коммит** |
| `X` | **Откатить** изменения в файле (Restore) |

### Другие команды
| Команда | Описание |
| :--- | :--- |
| `:Gdiffsplit` | Открыть сравнение (Diff) текущего файла |
| `:Gread` | Вернуть файл к состоянию последнего коммита |
| `:Gblame` | Показать, кто менял строки (сбоку) |

---

## 🛠 Системные команды

| Команда | Описание |
| :--- | :--- |
| `:Lazy` | **Менеджер плагинов** (обновить, установить, почистить) |
| `:Mason` | **Установщик серверов** LSP (установить Python, C++ и т.д.) |
| `:LspInfo` | Статус LSP (какой сервер подключен к текущему файлу) |
| `:checkhealth` | Проверка здоровья Neovim (если что-то сломалось) |

